<ul class="flex flex-col font-medium top-0">
	{% for item in items %}
		<li class="{{ item.classes | join(' ') }}">
			<a class="block py-2 pl-3 pr-4 text-white bg-transparent rounded" target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a>
		</li>
	{% endfor %}
</ul>
