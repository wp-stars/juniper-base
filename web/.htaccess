# BEGIN WordPress
# Die Anweisungen (Zeilen) zwischen „BEGIN WordPress“ und „END WordPress“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteBase /iwg/web/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /iwg/web/index.php [L]
</IfModule>

# END WordPress

#RewriteEngine on
#Redirect 301 /uploads/2022/09/IWG-Broschuere-Ansicht-Einzelseiten-1.pdf https://www.iwgplating.com/app/uploads/2022/09/IWG-Broschuere-Ansicht-Einzelseiten-1.pdf

RewriteEngine on
RewriteCond %{REQUEST_URI} ^/uploads/(.*)$
RewriteRule ^uploads/(.*)$ /app/uploads/$1 [R=301,L]